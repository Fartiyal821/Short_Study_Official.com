rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;
      // Replace this broad authenticated-write rule with admin custom claims or an allowlisted UID before production launch.
      allow write: if request.auth != null;
    }
  }
}

